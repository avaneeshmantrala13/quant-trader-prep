/**
 * `src/lib/mode/` — the pure Goal-Mode projection (WS0). Mode holds NO progress;
 * it selects/regroups the SAME `topicKey`-keyed mastery store for nav, course
 * grouping, feature visibility, gating priority, and dashboard focus.
 */
export * from "./goalMode";
export * from "./courseMap";
export * from "./visibility";
